import { atom } from "nanostores"

// Static Node Animation
export const inputValue = atom("42");

// Static Memory Animation

export const animStarted = atom(false);

export const arrayLimit = atom(5);
export const actualNode = atom(0);

//

