---
import { getCollection } from 'astro:content';
import HomePage from '../ui/layouts/HomePage.astro';

const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);
---

<HomePage {posts} />
